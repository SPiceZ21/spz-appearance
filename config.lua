-- spz-appearance/config.lua
Config = {}

-- No predefined uniform: players keep the appearance they built in the
-- fivem-appearance customization. Crew outfit still overrides when set.

Config.Debug = false
